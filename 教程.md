# BlueprintDL 完整使用教程

这份教程面向第一次接触 BlueprintDL 的读者。它会带你理解两件事：

1. 如何使用 BlueprintDL Core，通过配置组装并训练深度学习模型；
2. 如何在 BlueprintDL Forge 中创建一个边界清晰、组件可追踪、最终可独立交付的具体项目。

如果只想尽快跑起来，先完成第 1～4 节；如果要开发自己的项目，继续阅读第 8～12 节。

## 1. 先建立整体认识

BlueprintDL 不是单独的一套模型代码，而是“通用框架 + 项目锻造仓库”。

```text
BlueprintDL Core
    提供 Registry、Builder、Trainer 和可复用组件
                │
                ▼
projects/<project_id>
    在统一规则下开发具体业务项目
                │
                ▼
Forge audit / export
    审计通过后生成独立交付目录
```

### 1.1 Core 内部的四层结构

```text
Registry 层
    名字 -> 类或函数，并记录组件来源

Component 层
    Dataset、Transform、Model、Loss、Metric 等具体实现

Builder 层
    读取配置、查找 Registry、实例化组件、注入运行时依赖

Pipeline / Trainer 层
    编排数据、训练、验证、checkpoint 和 hook
```

这四层解决的是不同问题：

- Registry 回答“这个名字对应哪个实现”；
- Component 回答“这个功能具体怎么做”；
- Builder 回答“如何从配置得到一个可运行对象”；
- Trainer 回答“这些对象按照什么顺序运行”。

### 1.2 Forge 的四个区域

```text
standards/                 工程规则与交付门禁
dlkit/                     BlueprintDL Core
projects/<project_id>/     具体项目
dist/                      构建和导出产物
```

Core 不能导入具体项目，项目之间也不能互相引用内部代码。具体项目只通过 BlueprintDL 的公共 API 获得通用能力。

## 2. 环境准备：使用 uv

BlueprintDL 已使用 uv 管理 Python、依赖、虚拟环境和锁文件，不再使用 `requirements.txt` 作为依赖来源。

### 2.1 第一次同步环境

在仓库根目录打开 PowerShell：

```powershell
uv --version
uv sync
```

完成后，uv 会创建根项目的 `.venv`。平时不需要手动激活，直接在命令前加 `uv run`：

```powershell
uv run python --version
uv run python -c "import dlkit; print(dlkit.__version__)"
```

如果还没有安装 uv，见 [uv 使用说明](UV使用说明.md)。

### 2.2 GPU 与 CUDA 的边界

项目面向 GPU，但 BlueprintDL 不替你固定某个 CUDA、PyTorch wheel、驱动或显卡型号。训练前先检查当前环境：

```powershell
uv run python -c "import torch; print('torch:', torch.__version__); print('cuda build:', torch.version.cuda); print('gpu available:', torch.cuda.is_available())"
```

需要区分四件事：

| 层次 | 示例 |
| --- | --- |
| 应用配置 | `device: auto`、`device: cuda`、`amp: true` |
| 运行时发现 | GPU 是否存在、设备数量、显存和进程 rank |
| 依赖安装 | 选择与驱动兼容的 PyTorch/CUDA 构建 |
| 部署环境 | Docker 基础镜像、驱动接口和 GPU 挂载 |

架构代码和 `project.yaml` 不应写入某台机器的 CUDA 地址。安装或替换 GPU 环境是一项独立操作，不应在普通组件修改中顺手完成。

## 3. 跑通第一个分割模型

### 3.1 生成练习数据

```powershell
uv run python tools/make_toy_data.py --root data/toy --samples 120
```

它会生成训练集、验证集和类别信息。`data/` 默认不应提交到 Git。

### 3.2 训练 UNet

```powershell
uv run python tools/train.py --config configs/unet_toy.yaml
```

如果只想快速验证整个流程：

```powershell
uv run python tools/train.py --config configs/unet_toy.yaml --opts train.epochs=1
```

训练配置中的 `device: auto` 会根据运行环境解析设备；它不是“默认把项目设计成 CPU”。若你的运行合同要求必须使用 GPU，可以显式传入：

```powershell
uv run python tools/train.py --config configs/unet_toy.yaml --device cuda
```

### 3.3 评估

```powershell
uv run python tools/evaluate.py --config configs/unet_toy.yaml --checkpoint runs/unet_toy/best.pth
```

### 3.4 推理和可视化

```powershell
uv run python tools/predict.py --config configs/unet_toy.yaml --checkpoint runs/unet_toy/best.pth --input data/toy/val/images --palette data/toy/classes.json --output-dir runs/predict
```

训练输出默认位于 `runs/unet_toy/`：

| 文件 | 用途 |
| --- | --- |
| `config.yaml` | 本次训练的配置快照 |
| `metrics.jsonl` | 每一轮的 loss 和指标 |
| `last.pth` | 最后一轮 checkpoint |
| `best.pth` | 根据 `save_best` 选出的最优 checkpoint |
| `log.txt` | 运行日志 |

`runs/` 是运行产物，不属于源码，也不应默认进入项目交付包。

## 4. 看懂配置文件

以 `configs/unet_toy.yaml` 为例。

### 4.1 全局运行设置

```yaml
task: segmentation
seed: 42
device: auto
output_dir: runs/unet_toy
```

- `task` 用于说明任务语义；
- `seed` 控制随机种子；
- `device` 是逻辑设备策略；
- `output_dir` 是训练输出目录。

### 4.2 Dataset 与 Transform

```yaml
data:
  train:
    type: SegmentationDataset
    params:
      image_dir: data/toy/train/images
      mask_dir: data/toy/train/masks
      transform:
        type: Compose
        params:
          transforms:
            - type: RandomCrop
              params: { size: [128, 128] }
            - type: RandomHorizontalFlip
              params: { p: 0.5 }
            - type: ToTensor
  loader:
    batch_size: 8
    num_workers: 0
    pin_memory: false
```

`build_dataloaders` 先构造 Dataset，再把它注入 PyTorch DataLoader。检测等变长标注任务可以由 Dataset 提供自己的 `collate_fn`。

### 4.3 Model

```yaml
model:
  type: UNet
  params:
    in_channels: 3
    num_classes: 4
    backbone:
      type: SimpleCNN
      params:
        channels: [16, 32, 64, 128]
        num_blocks: [2, 2, 2, 2]
```

`UNet` 是完整模型，`SimpleCNN` 是嵌套的 backbone。UNet 会从 backbone 的 `out_channels` 推导 decoder 输入通道。

### 4.4 Loss 与 Metric

```yaml
loss:
  type: CombinedLoss
  params:
    losses:
      - type: CrossEntropyLoss
        params: { weight: 1.0, ignore_index: 255 }
      - type: DiceLoss
        params: { weight: 0.5, ignore_index: 255 }

metrics:
  - type: SegMetrics
    params: { num_classes: 4, ignore_index: 255 }
```

Loss 遵守 `loss(predictions, batch) -> scalar`。Metric 遵守 `reset()`、`update()` 和 `compute()`。

### 4.5 Optimizer 与 Scheduler

```yaml
optimizer:
  type: AdamW
  params:
    lr: 0.001
    weight_decay: 0.0001

scheduler:
  type: CosineAnnealingLR
  params:
    T_max: 20
```

配置只说明名称和普通参数。Trainer 的构造顺序是：

```text
Model
  -> Optimizer(model.parameters())
  -> Scheduler(optimizer)
```

因此 optimizer 和 scheduler 使用专门构造函数是合理的：它们仍然从配置选择 Registry 实现，但运行时依赖必须等上游对象创建后再注入。

### 4.6 Train

```yaml
train:
  epochs: 20
  amp: false
  gradient_clip: 1.0
  log_interval: 10
  save_best: { name: mIoU, mode: max }
```

当 `amp: true` 且解析后的 device 是 CUDA 时，Trainer 才启用自动混合精度。

## 5. Registry 与 `type + params`

BlueprintDL 内置以下语义注册表：

```text
BACKBONES  DECODERS  HEADS  MODELS  LOSSES
DATASETS   TRANSFORMS  CLEANERS  METRICS
OPTIMIZERS SCHEDULERS
```

普通配置节点使用：

```yaml
type: 注册名称
params:
  参数名: 参数值
```

`build_from_cfg` 的规则是：

1. 普通值原样返回；
2. 列表和元组逐项构造；
3. 含有 `type` 的字典会查找注册实现并实例化；
4. 普通字典递归处理每个值；
5. `_manual_build = True` 的组件自行处理其局部嵌套配置。

注册名在多个 Registry 中重复时，必须使用限定名称：

```yaml
type: models.Example
```

未知名称错误会显示可用名称；重复注册默认立即报错，不会静默覆盖。

### 5.1 查看已注册组件

```powershell
uv run blueprintdl-forge components --provider blueprintdl
uv run blueprintdl-forge components --provider blueprintdl --registry models
uv run blueprintdl-forge components --provider blueprintdl --registry losses --json
```

每条记录包含 Registry、注册名、provider、模块、限定名称、标签和说明。Core 中的 `dlkit.*` 实现自动归属 `blueprintdl`。

## 6. 任务无关协议

Trainer 不根据任务名称维护分割、分类、检测和深度的巨大分支，而是依赖组件协议：

| 角色 | 约定 |
| --- | --- |
| Dataset | 样本为字典，至少包含 `image` |
| Model | `forward(image) -> predictions` |
| Loss | `forward(predictions, batch) -> scalar` |
| Metric | `reset()`、`update(predictions, batch)`、`compute() -> dict` |
| Batch | 标签保留在任务自己的键中，例如 `mask`、`label`、`boxes` |

当前示例：

| 任务 | Dataset | Model | Loss | Metric |
| --- | --- | --- | --- | --- |
| 分割 | `SegmentationDataset` | `UNet` / `DeepLabV3` / `DeepLabV3Plus` | CE / Dice / Focal | `SegMetrics` |
| 分类 | `ClassificationDataset` | `Classifier` | `CrossEntropyLoss` | `Accuracy` |
| 检测 | `DetectionDataset` | `FCOS` | `FCOSLoss` | `DetMetrics` |
| 深度 | `DepthDataset` | `DepthNet` | `DepthLoss` | `DepthMetrics` |

只要新任务的组件满足协议，Trainer 就不需要知道它的具体类型。

## 7. 使用自己的数据与其他示例

### 7.1 分割数据

默认分割 Dataset 使用图片与 mask 文件名对应的结构：

```text
dataset/
├── train/
│   ├── images/
│   └── masks/
└── val/
    ├── images/
    └── masks/
```

mask 是单通道类别索引图，每个像素值代表类别 ID。修改配置中的 `image_dir`、`mask_dir`、模型类别数和指标类别数即可。

### 7.2 分类

```powershell
uv run python tools/make_toy_cls_data.py --root data/cls_toy --samples-per-class 30
uv run python tools/train.py --config configs/cls_toy.yaml
```

### 7.3 检测

```powershell
uv run python tools/make_toy_det_data.py --root data/det_toy --samples 120
uv run python tools/train.py --config configs/det_toy.yaml
uv run python tools/predict_det.py --config configs/det_toy.yaml --checkpoint runs/det_toy/best.pth --input data/det_toy/val/images --classes data/det_toy/classes.json --output-dir runs/predict_det
```

### 7.4 深度估计

```powershell
uv run python tools/make_toy_depth_data.py --root data/depth_toy --samples 120
uv run python tools/train.py --config configs/depth_toy.yaml
uv run python tools/predict_depth.py --config configs/depth_toy.yaml --checkpoint runs/depth_toy/best.pth --input data/depth_toy/val/images --output-dir runs/predict_depth
```

### 7.5 数据清洗

```powershell
uv run python tools/clean_data.py --config configs/clean_toy.yaml
```

初次检查数据时优先使用 `action: report`。移动和删除数据是有状态操作，必须先核对报告和精确目标。

### 7.6 ColonCrafter

`configs/coloncrafter.yaml` 是研究型内镜深度推理示例，依赖独立的第三方实现和模型资产。它不属于默认环境，不会在普通 `uv sync`、Registry 检查或 Forge audit 中自动下载。

## 8. 新增一个 Core 组件

只有真正通用、接口稳定、不带具体项目业务假设的组件才应该放进 `dlkit/`。

下面以 Loss 为例：

```python
import torch.nn as nn

from dlkit.registry import LOSSES


@LOSSES.register(
    name="MyRobustLoss",
    tags=("segmentation",),
    description="Example project-neutral loss",
)
class MyRobustLoss(nn.Module):
    def __init__(self, weight=1.0, target_key="mask"):
        super().__init__()
        self.weight = weight
        self.target_key = target_key

    def forward(self, predictions, batch):
        target = batch[self.target_key]
        value = ...
        return self.weight * value
```

Core 组件接入步骤：

1. 在正确的 `dlkit` 组件目录实现类；
2. 使用对应 Registry 注册；
3. 在该包的显式导入入口中加载模块；
4. 添加配置示例或测试配置；
5. 测试直接调用、Registry 查找和配置构造；
6. 用 `components --provider blueprintdl` 检查来源。

不要把每个小函数都注册化。只有需要配置选择、独立扩展或测试替换的稳定角色才适合作为插件点。

## 9. 新增一个具体项目组件

项目专属 Dataset、模型、Loss、Metric、标注解析和业务后处理应放在 `projects/<project_id>/src/<package>/`，不要提前提升到 Core。

以 `polypmeasure` 的 BoundaryLoss 为例。

### 9.1 创建实现文件

文件：`projects/polypmeasure/src/polypmeasure/components/losses.py`

```python
import torch.nn as nn

from dlkit.registry import LOSSES


@LOSSES.register(
    name="BoundaryLoss",
    provider="polypmeasure",
    tags=("segmentation", "boundary"),
)
class BoundaryLoss(nn.Module):
    def __init__(self, weight=1.0):
        super().__init__()
        self.weight = weight

    def forward(self, predictions, batch):
        mask = batch["mask"]
        value = ...
        return self.weight * value
```

这里的 `provider` 必须与 `project.yaml` 中的 `project.id` 完全一致。

### 9.2 加入显式 bootstrap

修改 `projects/polypmeasure/src/polypmeasure/components/__init__.py`：

```python
def load_components():
    from . import losses

    return (losses.__name__,)
```

项目根包被导入时不应自动注册所有内容。Forge 只调用 `project.yaml` 中声明的：

```yaml
plugins:
  bootstrap:
    - polypmeasure.bootstrap:register
```

bootstrap 只能导入组件，不能加载 checkpoint、扫描数据、初始化 GPU 或开始训练。

### 9.3 检查归属

```powershell
uv run blueprintdl-forge inspect polypmeasure
uv run blueprintdl-forge audit polypmeasure
```

不要在 `project.yaml` 里再手写一份组件清单。Forge 会从实际 Registry provenance 生成，避免代码和文档漂移。

### 9.4 项目训练入口

具体项目应拥有自己的 Composition Root。它负责：

1. 调用项目 bootstrap；
2. 加载并校验项目配置；
3. 解析运行环境；
4. 调用 Builder 构造对象；
5. 启动 Trainer 或项目 Pipeline。

不要要求根目录的通用 `tools/train.py` 猜测并扫描所有项目插件。显式项目入口更可追踪，也更容易在项目离开 Forge 后独立运行。

## 10. 创建一个新项目

在根目录执行：

```powershell
uv run blueprintdl-forge new retinal-seg --display-name "Retinal Segmentation"
```

默认生成：

```text
projects/retinal-seg/
├── project.yaml
├── pyproject.toml
├── README.md
├── configs/
├── src/retinal_seg/
│   ├── __init__.py
│   ├── bootstrap.py
│   └── components/__init__.py
└── tests/
```

创建后建议按顺序完成：

1. 在 README 定义任务、数据协议和交付目标；
2. 决定哪些能力复用 Core，哪些属于项目；
3. 在项目包内实现真实组件；
4. 通过 bootstrap 显式加载；
5. 添加配置和项目测试；
6. 运行 `inspect` 与 `audit`；
7. 成熟后执行构建和导出。

项目 ID 使用小写字母、数字和连字符；Python 包名使用合法的小写模块名。Forge 会拒绝覆盖已经存在的项目目录。

## 11. 理解 `project.yaml`

示例：

```yaml
schema_version: 1

project:
  id: polypmeasure
  display_name: PolypMeasure
  package: polypmeasure
  version: 0.1.0

framework:
  package: blueprintdl
  version: ">=0.1,<0.2"

plugins:
  bootstrap:
    - polypmeasure.bootstrap:register

delivery:
  include:
    - pyproject.toml
    - project.yaml
    - README.md
    - src
    - configs
    - tests
```

字段职责：

| 字段 | 作用 |
| --- | --- |
| `project.id` | Forge 项目标识和 provider |
| `project.package` | Python 导入包名 |
| `project.version` | 项目版本，需与 `pyproject.toml` 一致 |
| `framework` | 兼容的 BlueprintDL Core 版本范围 |
| `plugins.bootstrap` | 允许执行的显式注册入口 |
| `delivery.include` | 可进入交付目录的白名单 |

依赖只在 `pyproject.toml` 中维护；device、凭据、对象实例、本机路径和 CUDA 构建不属于项目清单。

## 12. Forge 命令完整用法

### 12.1 列出项目

```powershell
uv run blueprintdl-forge list
uv run blueprintdl-forge list --json
```

### 12.2 查看组件

```powershell
uv run blueprintdl-forge components --provider blueprintdl
uv run blueprintdl-forge components --registry models
```

### 12.3 查看一个项目提供的组件

```powershell
uv run blueprintdl-forge inspect polypmeasure
uv run blueprintdl-forge inspect polypmeasure --json
```

`inspect` 会显式执行该项目的 bootstrap，然后只列出 `provider=project.id` 的记录。

### 12.4 审计

```powershell
uv run blueprintdl-forge audit polypmeasure
```

审计内容包括：

- 项目目录和 ID 是否一致；
- `pyproject.toml` 与 manifest 是否一致；
- 源码包和 bootstrap 是否存在；
- delivery 路径是否安全并存在；
- 是否导入其他项目内部包；
- bootstrap 是否能加载；
- 项目组件是否声明正确 provider。

只检查文件、不加载插件时：

```powershell
uv run blueprintdl-forge audit polypmeasure --skip-plugins
```

### 12.5 导出

```powershell
uv run blueprintdl-forge export polypmeasure dist/projects
```

输出位于 `dist/projects/polypmeasure/`。Forge：

- 先执行完整审计；
- 只复制 `delivery.include` 白名单；
- 排除 `.venv`、缓存、字节码和构建噪音；
- 拒绝覆盖已有目标；
- 生成 `.blueprintdl-export.json` 回执。

当前交付模式是“项目包 + 指定版本范围的 BlueprintDL Core 依赖”。导出成功不代表目标环境已经拥有 Core；交付方仍需发布 Core 包或随交付提供经过审计的 Core 构建物。

## 13. 项目环境为什么彼此独立

多个项目可以位于同一个 Git 仓库，但不一定应共享一个 uv workspace、锁文件和虚拟环境。

GPU 项目之间可能存在：

- 不同 Python 版本；
- 不同 PyTorch/CUDA/ROCm 组合；
- 不同客户部署限制；
- 不同依赖源和许可证要求。

因此，Forge 默认让每个具体项目拥有自己的 `pyproject.toml`，而不自动加入根 workspace。

仓库内使用 Forge 检查项目骨架和组件归属。项目要在独立环境中安装运行时，需要确保其声明的 BlueprintDL Core 版本已经可以通过包源或交付构建物获得。不要使用长期 `sys.path` 技巧掩盖未声明依赖。

## 14. Trainer、Hook 与续训

### 14.1 代码方式训练 Core 示例

```python
from dlkit.engine.trainer import Trainer
from dlkit.utils.config import load_config

cfg = load_config("configs/unet_toy.yaml")
trainer = Trainer(cfg, work_dir="runs/my_run")
history = trainer.train()
```

### 14.2 Hook

可用事件：

```text
before_train
after_train
before_epoch
after_epoch
before_train_step
after_train_step
```

示例：

```python
def report_epoch(trainer, epoch=None, metrics=None, **kwargs):
    print(epoch, metrics)


trainer.register_hook("after_epoch", report_epoch)
trainer.train()
```

### 14.3 Callback

```python
from dlkit.engine.callbacks import EarlyStopping

EarlyStopping(
    metric="mIoU",
    mode="max",
    patience=10,
).attach(trainer)
```

### 14.4 断点续训

```powershell
uv run python tools/train.py --config configs/unet_toy.yaml --resume runs/unet_toy/last.pth
```

checkpoint 会恢复模型、optimizer、scheduler、epoch 和指标历史。

## 15. 模型导出

安装导出可选依赖：

```powershell
uv sync --extra export
```

使用目标 GPU 导出 ONNX：

```powershell
uv run python tools/export.py --config configs/unet_toy.yaml --checkpoint runs/unet_toy/best.pth --format onnx --device cuda --output runs/unet_toy/model.onnx
```

导出 TorchScript：

```powershell
uv run python tools/export.py --config configs/unet_toy.yaml --checkpoint runs/unet_toy/best.pth --format torchscript --device cuda --output runs/unet_toy/model.pt
```

导出所用 device 必须与当前 PyTorch 构建和硬件环境兼容。模型部署策略与训练架构是不同层次，应在目标环境单独验证。

## 16. 测试与验收

### 16.1 Core 测试

```powershell
uv run pytest -q
```

### 16.2 项目测试

具体项目测试与 Core 测试应分开运行，防止另一个项目的环境掩盖未声明依赖。项目尚未独立安装时，可以先通过根 Forge 审计其结构和注册来源；进入交付阶段后必须在项目独立环境或导出目录中运行项目测试。

### 16.3 构建

```powershell
uv build
uv build --project projects/polypmeasure
```

应分别检查 Core 和项目的 wheel/sdist。Core 构建物不能包含所有 `projects/`。首次构建时，uv 可能需要获取 `pyproject.toml` 声明的构建后端 `hatchling`；只有构建依赖已经进入缓存时，`uv build --offline` 才能成功。

### 16.4 一个组件任务的完成标准

- [ ] 放在正确的 Core 或项目目录；
- [ ] 使用正确 Registry；
- [ ] 项目组件声明正确 provider；
- [ ] bootstrap 可以显式加载；
- [ ] 配置只含可序列化参数；
- [ ] 运行时对象通过 Builder 注入；
- [ ] 单元测试实际通过；
- [ ] Forge audit 通过；
- [ ] 没有修改无关项目或硬件环境。

## 17. 常见问题

### `No module named 'dlkit'`

确认当前目录是仓库根目录，并执行：

```powershell
uv sync
uv run python -c "import dlkit; print(dlkit.__version__)"
```

不要依赖系统 Python 或长期修改 `PYTHONPATH`。

### `not registered in any registry`

可能原因：

- 配置中的 `type` 拼错；
- 组件模块没有被显式导入；
- 具体项目 bootstrap 没有执行；
- 组件被注册到了错误的 Registry。

先运行：

```powershell
uv run blueprintdl-forge components --provider blueprintdl
uv run blueprintdl-forge inspect <project_id>
```

### `type` 出现歧义

如果同名组件存在于多个 Registry，使用限定名称，例如 `models.Example` 或 `losses.Example`。

### 为什么 optimizer 和 scheduler 不是普通递归构造

因为 optimizer 需要已经构建的 `model.parameters()`，scheduler 需要已经构建的 optimizer。名称和普通参数仍然来自配置，运行时对象由专用 Builder 注入。

### 为什么 `torch.cuda.is_available()` 是 `False`

这通常是运行环境、驱动或 PyTorch 构建的问题，不是把 YAML 改成 `cuda` 就能解决。按照 [uv 使用说明](UV使用说明.md) 的 GPU 边界检查环境。

### 为什么不能直接 `uv sync --project projects/polypmeasure`

`polypmeasure` 声明依赖兼容版本的 `blueprintdl`。如果 Core 尚未发布到项目可访问的包源，也没有提供本地交付构建物，独立解析自然无法找到该依赖。仓库开发期可先使用 Forge 进行结构和组件审计；独立运行前应明确 Core 的发布或随包交付方式。

### Forge export 报目标已存在

这是保护机制。Forge 不会把新交付物合并进旧目录。选择新的目标目录，或在确认旧交付物不再需要后由你显式处理它。

### 项目组件 audit 报 provider 错误

检查注册装饰器中的 `provider` 是否与 `project.yaml` 的 `project.id` 完全一致。

## 18. 推荐学习路线

1. 用 `unet_toy.yaml` 跑通生成数据、训练、评估和推理；
2. 修改 `--opts`，观察配置如何影响对象构造；
3. 用 `components` 查看 Registry 和 provider；
4. 阅读 `dlkit/registry.py` 与 `dlkit/engine/trainer.py`；
5. 切换分类、检测和深度配置，理解任务无关协议；
6. 在测试中实现一个最小 Core Loss 并从配置构造；
7. 在 `polypmeasure` 中实现一个带 provider 的项目组件；
8. 运行 `inspect` 和 `audit`；
9. 创建第二个练习项目，验证项目间边界；
10. 审计并导出一个项目，在导出目录中重新验证安装和测试。

完成这条路线后，你学到的不只是如何训练一个模型，还会理解如何让多个深度学习项目在同一套规则下持续演化，并保持可追踪、可测试和可交付。
