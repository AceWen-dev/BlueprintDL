# dlkit 框架完整使用教程

> 一个「积木 + 说明书」式的模块化深度学习流水线框架。
> 你只需要改配置文件（说明书），就能搭出 UNet、DeepLabV3、DeepLabV3+ 等网络，
> 并完成 **数据清洗 → 训练 → 评估 → 可视化 → 导出部署** 一条龙。

---

## 目录

1. [核心思想：积木 + 说明书](#1-核心思想积木--说明书)
2. [目录结构速查](#2-目录结构速查)
3. [环境准备](#3-环境准备)
4. [快速上手：跑通第一个模型](#4-快速上手机跑通第一个模型)
5. [核心概念：注册中心与 type/params](#5-核心概念注册中心与-typeparams)
6. [配置文件逐段详解](#6-配置文件逐段详解)
7. [模型三段式：backbone + decoder + head](#7-模型三段式backbone--decoder--head)
8. [内置模块清单](#8-内置模块清单)
9. [换成你自己的数据](#9-换成你自己的数据)
10. [如何新增一个模块](#10-如何新增一个模块)
11. [训练引擎、钩子与回调](#11-训练引擎钩子与回调)
12. [可视化](#12-可视化)
13. [部署：ONNX / TorchScript](#13-部署onnx--torchscript)
14. [训练产物说明](#14-训练产物说明)
15. [常见问题 FAQ](#15-常见问题-faq)

---

## 1. 核心思想：积木 + 说明书

整个框架只有两个东西：

| 概念 | 对应 | 说明 |
|------|------|------|
| **积木** | `dlkit/` 里的模块 | 骨干网络、损失函数、评价指标、数据增强……都写好了 |
| **说明书** | `configs/*.yaml` | 你只写「用哪块积木 + 参数是什么」，框架自动拼装 |

**你几乎不用改代码，改配置文件就能换网络、换损失、换数据。**

一个最典型的配置长这样：

```yaml
model:
  type: UNet              # 用注册中心里叫 "UNet" 的积木
  params:
    num_classes: 4        # 参数：4 个类别
```

想换网络？把 `type: UNet` 改成 `type: DeepLabV3Plus` 即可，其余不用动。

---

## 2. 目录结构速查

```
BlueprintDL/
├── configs/               # 说明书（配置文件），你主要改这里
│   ├── unet_toy.yaml
│   ├── deeplabv3_toy.yaml
│   ├── deeplabv3plus_toy.yaml
│   └── clean_toy.yaml
├── dlkit/                 # 框架本体（积木仓库），一般不碰
│   ├── registry.py        # 注册中心：即插即用的核心
│   ├── data/              # 数据：清洗 / 数据集 / 增强 / 加载器
│   ├── models/            # 模型：backbone / decoder / head / loss / 完整网络
│   ├── metrics/           # 评价指标
│   ├── engine/            # 训练引擎 + 钩子 + 回调
│   ├── visualize/         # 可视化
│   └── deploy/            # 导出与推理
├── tools/                 # 命令行入口脚本
│   ├── train.py           # 训练
│   ├── evaluate.py        # 评估
│   ├── predict.py         # 推理 + 可视化
│   ├── clean_data.py      # 数据清洗
│   ├── export.py          # 导出 ONNX / TorchScript
│   └── make_toy_data.py   # 生成合成数据（练习用）
├── tests/                 # 测试
├── data/                  # 你的数据（默认被 gitignore）
├── runs/                  # 训练产物（权重 / 日志 / 指标）
└── README.md              # 项目说明
```

---

## 3. 环境准备

框架依赖 PyTorch。你已有一个现成环境 `pytorch_projects_test`，两种用法任选：

**方法 A：激活环境（推荐）**

```powershell
conda activate pytorch_projects_test
```

> 如果 `conda` 命令找不到，先初始化：
> `C:\Users\Franco\miniconda3\Scripts\conda.exe init powershell`，然后重开终端。

**方法 B：直接用环境里的 python 全路径**

```powershell
C:\Users\Franco\miniconda3\envs\pytorch_projects_test\python.exe
```

下文命令统一用 `python` 表示，实际用哪种取决于你上面的选择。

依赖清单见 `requirements.txt`。你的环境基本都装好了，**只有 matplotlib 需要修复**（可视化才会用到）：

```powershell
python -m pip install --force-reinstall matplotlib
```

---

## 4. 快速上手：跑通第一个模型

在项目根目录依次执行（每次一条，等它跑完）：

```powershell
# ① 造一批假数据：4 类（背景/圆/方块/三角），放到 data/toy/
python tools/make_toy_data.py --root data/toy --samples 120

# ② 训练 UNet，20 个 epoch，产物在 runs/unet_toy/
python tools/train.py --config configs/unet_toy.yaml

# ③ 评估模型，输出 mIoU / mDice / mAcc 等分数
python tools/evaluate.py --config configs/unet_toy.yaml --checkpoint runs/unet_toy/best.pth

# ④ 推理并可视化（生成 mask 图和叠加图）
python tools/predict.py --config configs/unet_toy.yaml `
    --checkpoint runs/unet_toy/best.pth `
    --input data/toy/val/images `
    --palette data/toy/classes.json `
    --output-dir runs/predict
```

**先只跑 ①② ③**，成功了再往下看。

如果训练太慢（CPU 版），可以先用少量数据 + 少量 epoch 试通流程：

```powershell
python tools/make_toy_data.py --root data/toy --samples 40
python tools/train.py --config configs/unet_toy.yaml --opts train.epochs=1
```

`--opts` 是「临时覆盖配置」，格式 `键=值`，用 `.` 表示层级，例如：

```powershell
--opts train.epochs=50 optimizer.params.lr=0.0001 model.params.num_classes=8
```

---

## 5. 核心概念：注册中心与 type/params

### 5.1 什么是注册中心

框架里有若干个「注册表」，每个注册表装一类积木：

```
BACKBONES  DECODERS  HEADS  MODELS  LOSSES
DATASETS   TRANSFORMS  CLEANERS  METRICS
OPTIMIZERS  SCHEDULERS
```

写模块时用装饰器把它「挂」到对应的注册表：

```python
from dlkit.registry import BACKBONES
import torch.nn as nn

@BACKBONES.register()          # 挂到 BACKBONES 注册表
class MyBackbone(nn.Module):
    ...
```

之后配置里写 `type: MyBackbone`，框架就能按名字找到它。

### 5.2 type + params 的组装规则

配置文件里所有「积木」都遵循同一种写法：

```yaml
名字:
  type: 积木名          # 注册表里的类名
  params:               # 构造参数
    参数1: 值
    参数2: 值
```

框架的 `build_from_cfg` 会**递归**地构建嵌套结构。例如这个 loss：

```yaml
loss:
  type: CombinedLoss
  params:
    losses:
      - { type: CrossEntropyLoss, params: { weight: 1.0, ignore_index: 255 } }
      - { type: DiceLoss, params: { weight: 0.5, ignore_index: 255 } }
```

框架会自动先构建里面的 `CrossEntropyLoss` 和 `DiceLoss`，再传给 `CombinedLoss`。**所以你可以无限嵌套、自由组合**，这正是「即插即用」的含义。

### 5.3 一个配置 = 一条流水线

配置从上到下就是完整流程的 7 个环节：

```
data（数据） → model（模型） → loss（损失） → optimizer（优化器）
→ scheduler（学习率） → train（训练设置） → metrics（评估指标）
```

---

## 6. 配置文件逐段详解

以 `configs/unet_toy.yaml` 为例，逐段解释每个字段。

### 6.1 顶层

```yaml
task: segmentation      # 任务类型（当前只支持分割）
seed: 42                # 随机种子，保证可复现
device: auto            # auto=有 GPU 用 GPU，否则 CPU；也可写 cpu / cuda
output_dir: runs/unet_toy   # 训练产物输出目录
```

### 6.2 data（数据）

```yaml
data:
  train:                          # 训练集
    type: SegmentationDataset     # 数据集类型
    params:
      image_dir: data/toy/train/images   # 图片目录
      mask_dir: data/toy/train/masks     # 标签目录
      transform:                  # 数据增强流水线（按顺序执行）
        type: Compose
        params:
          transforms:
            - type: RandomCrop            # 随机裁剪到 128x128
              params: { size: [128, 128] }
            - type: RandomHorizontalFlip  # 50% 概率水平翻转
              params: { p: 0.5 }
            - type: RandomBrightnessContrast  # 亮度对比度抖动
              params: { alpha: [0.8, 1.2], beta: [-0.1, 0.1] }
            - type: ToTensor              # 转成张量（放后面）
            - type: Normalize             # 归一化（必须在 ToTensor 之后）
              params: { mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225] }
  val:                            # 验证集（只做 Resize，不做随机增强）
    type: SegmentationDataset
    params:
      image_dir: data/toy/val/images
      mask_dir: data/toy/val/masks
      transform:
        type: Compose
        params:
          transforms:
            - type: Resize
              params: { size: [128, 128] }
            - type: ToTensor
            - type: Normalize
              params: { mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225] }
  loader:                         # DataLoader 参数
    batch_size: 8                 # 批大小
    num_workers: 0                # 数据加载进程数（Windows 建议 0）
    pin_memory: false
```

**重要规则**：几何变换（裁剪/翻转/旋转）在 `ToTensor` 之前，`Normalize` 在 `ToTensor` 之后。

### 6.3 model（模型）

```yaml
model:
  type: UNet               # 完整网络，三种内置：UNet / DeepLabV3 / DeepLabV3Plus
  params:
    in_channels: 3         # 输入通道数（RGB=3，灰度=1）
    num_classes: 4         # 类别数（含背景）
    backbone:              # 骨干网络
      type: SimpleCNN
      params:
        channels: [16, 32, 64, 128]   # 每层输出通道数
        num_blocks: [2, 2, 2, 2]      # 每层卷积块数
```

`decoder` 和 `head` 没写，框架会自动配默认值。如果你想自己指定：

```yaml
    decoder:
      type: UNetDecoder
      # params 可省略，框架自动根据 backbone 通道数推断
    head:
      type: SegHead
```

### 6.4 loss（损失）

```yaml
loss:
  type: CombinedLoss              # 组合损失：CE + 0.5×Dice
  params:
    losses:
      - { type: CrossEntropyLoss, params: { weight: 1.0, ignore_index: 255 } }
      - { type: DiceLoss, params: { weight: 0.5, ignore_index: 255 } }
```

只用单个损失可以简写：

```yaml
loss:
  type: CrossEntropyLoss
  params: { ignore_index: 255 }
```

> `ignore_index: 255` 表示标签里值为 255 的像素不参与计算（很多公开数据集用它标记「忽略区域」）。

### 6.5 optimizer（优化器）

```yaml
optimizer:
  type: AdamW                     # 可选 Adam / AdamW / SGD
  params: { lr: 0.001, weight_decay: 0.0001 }
```

### 6.6 scheduler（学习率调度）

```yaml
scheduler:
  type: CosineAnnealingLR         # 可选 StepLR / MultiStepLR / CosineAnnealingLR / PolyLR
  params: { T_max: 20 }
```

> `PolyLR` 是分割任务常用的「多项式衰减」，每个 step 衰减一次，适合 DeepLab：

```yaml
scheduler:
  type: PolyLR
  params: { power: 0.9 }
```

### 6.7 train（训练设置）

```yaml
train:
  epochs: 20                     # 训练轮数
  amp: false                     # 混合精度（仅 GPU 有效，CPU 忽略）
  gradient_clip: 1.0             # 梯度裁剪，防止梯度爆炸
  log_interval: 10               # 每多少个 batch 打印一次 loss
  save_best: { name: mIoU, mode: max }   # 按哪个指标、以什么方向保存最优模型
```

### 6.8 metrics（评估指标）

```yaml
metrics:
  - type: SegMetrics
    params: { num_classes: 4, ignore_index: 255 }
```

输出的指标有：`mIoU`（平均交并比，分割最常用）、`mDice`、`mAcc`、`PixAcc`（像素准确率）、`FWIoU`。

---

## 7. 模型三段式：backbone + decoder + head

所有分割网络都可以拆成三段：

```
输入图片 → backbone（编码：逐步下采样、提取特征）
         → decoder（解码：恢复分辨率、融合特征）
         → head（输出：得到每个像素的类别分数）
```

| 网络 | backbone | decoder | 特点 |
|------|----------|---------|------|
| **UNet** | 任意 | UNetDecoder（上采样 + 跳跃连接） | 医学图像经典，结构简单 |
| **DeepLabV3** | 任意 | ASPP（空洞空间金字塔池化） | 多尺度上下文 |
| **DeepLabV3+** | 任意 | ASPP + 低层特征融合 | 更精细的边缘 |

**内置 backbone：**

| 类型 | 说明 |
|------|------|
| `SimpleCNN` | 手写，轻量，适合学习/小数据 |
| `ResNet` | 手写，支持 18/34/50/101，可加载 torchvision 预训练 |
| `TorchvisionBackbone` | 直接用 torchvision 预训练模型 |

换成预训练 ResNet50 的例子：

```yaml
model:
  type: DeepLabV3Plus
  params:
    num_classes: 19
    backbone:
      type: TorchvisionBackbone
      params:
        name: resnet50
        pretrained: true
        output_stride: 16
```

> 每个 backbone 都必须有 `out_channels` 属性（各阶段输出通道列表），框架靠它自动推断 decoder 的输入通道数。这就是「即插即用」的关键约定。

---

## 8. 内置模块清单

| 注册表 | 内置积木 |
|--------|----------|
| BACKBONES | `SimpleCNN` `ResNet` `TorchvisionBackbone` |
| DECODERS | `UNetDecoder` `ASPP` |
| HEADS | `SegHead` |
| MODELS | `UNet` `DeepLabV3` `DeepLabV3Plus` |
| LOSSES | `CrossEntropyLoss` `DiceLoss` `FocalLoss` `CombinedLoss` |
| DATASETS | `SegmentationDataset` |
| TRANSFORMS | `Compose` `Resize` `CenterCrop` `RandomCrop` `RandomHorizontalFlip` `RandomVerticalFlip` `RandomRotate90` `RandomBrightnessContrast` `ToTensor` `Normalize` |
| CLEANERS | `CorruptImage` `SizeMismatch` `MaskValues` `MinSize` `Duplicate` |
| METRICS | `SegMetrics` |
| OPTIMIZERS | `Adam` `AdamW` `SGD` |
| SCHEDULERS | `StepLR` `MultiStepLR` `CosineAnnealingLR` `PolyLR` |

---

## 9. 换成你自己的数据

### 9.1 数据格式

只需要两个文件夹，**文件名一一对应**：

```
你的数据集/
├── images/   001.png  002.png  003.png ...
└── masks/    001.png  002.png  003.png ...
```

- `images/`：普通 RGB 图片。
- `masks/`：**单通道灰度图**，每个像素的值 = 类别编号（0=背景，1、2、3…）。
  例如 `001.png` 里某个像素值是 2，表示该像素属于第 2 类。

### 9.2 改配置

```yaml
data:
  train:
    params:
      image_dir: 你的数据集/train/images
      mask_dir:  你的数据集/train/masks
  val:
    params:
      image_dir: 你的数据集/val/images
      mask_dir:  你的数据集/val/masks
```

同时改：

```yaml
model.params.num_classes: 你的类别数（含背景）
metrics.params.num_classes: 你的类别数
```

### 9.3 数据清洗（可选）

清洗配置见 `configs/clean_toy.yaml`，可检测：损坏图片、图标签尺寸不匹配、非法标签值、图片过小、重复图片。

```powershell
python tools/clean_data.py --config configs/clean_toy.yaml
```

其中 `action` 有三个选项：
- `report`：只生成报告，不动文件（默认，最安全）
- `move`：把问题文件移到 `quarantine/` 隔离
- `delete`：直接删除问题文件

---

## 10. 如何新增一个模块

这就是框架最核心的能力：**写一个类 + 一个装饰器 = 新积木**，之后配置里直接引用。

### 10.1 新增一个 backbone

```python
# 文件：my_modules.py（随便放哪，训练前 import 一下即可）
import torch.nn as nn
from dlkit.registry import BACKBONES

@BACKBONES.register()
class MyBackbone(nn.Module):
    def __init__(self, in_channels=3):
        super().__init__()
        self.conv1 = nn.Conv2d(in_channels, 16, 3, 2, 1)
        self.conv2 = nn.Conv2d(16, 32, 3, 2, 1)
        self.conv3 = nn.Conv2d(32, 64, 3, 2, 1)
        self.out_channels = [16, 32, 64]   # 必须：各阶段输出通道数

    def forward(self, x):
        f1 = self.conv1(x)
        f2 = self.conv2(f1)
        f3 = self.conv3(f2)
        return [f1, f2, f3]                # 返回多尺度特征列表
```

配置里使用：

```yaml
backbone:
  type: MyBackbone
  params: { in_channels: 3 }
```

### 10.2 新增一个损失函数

```python
import torch.nn as nn
import torch.nn.functional as F
from dlkit.registry import LOSSES

@LOSSES.register()
class MyLoss(nn.Module):
    def __init__(self, weight=1.0, ignore_index=None):
        super().__init__()
        self.weight = weight
        self.ignore_index = ignore_index

    def forward(self, logits, target):
        # logits: (B, C, H, W) 模型输出；target: (B, H, W) 标签
        return self.weight * F.cross_entropy(
            logits, target, ignore_index=self.ignore_index)
```

### 10.3 新增一个数据增强

```python
import random
import numpy as np
from dlkit.registry import TRANSFORMS

@TRANSFORMS.register()
class MyNoise:
    def __init__(self, sigma=0.02):
        self.sigma = sigma

    def __call__(self, sample):
        image = sample['image']
        noise = np.random.randn(*image.shape) * self.sigma * 255
        sample['image'] = np.clip(image + noise, 0, 255).astype(np.uint8)
        return sample
```

配置里加到 transforms 列表即可。

### 10.4 新增一个完整模型（进阶）

复制内置模型的写法即可，关键是两点：
1. 加 `_manual_build = True`（告诉框架：组件我自己来组装，这样才能注入派生通道数）。
2. 用 `_as(...)` 构建 backbone/decoder/head，并自动补全缺省通道。

```python
import torch.nn.functional as F
from dlkit.registry import MODELS, build_from_cfg
from dlkit.models.base import BaseModel

@MODELS.register()
class MyModel(BaseModel):
    _manual_build = True

    def __init__(self, in_channels=3, num_classes=1, backbone=None, head=None):
        super().__init__()
        self.backbone = _as(backbone, in_channels=in_channels)
        self.head = _as(head, in_channels=self.backbone.out_channels[-1], num_classes=num_classes)
        if not getattr(self.backbone, 'pretrained', False):
            self.init_weights()

    def forward(self, x):
        feats = self.backbone(x)
        out = feats[-1]
        logits = self.head(out)
        return F.interpolate(logits, size=x.shape[2:], mode='bilinear', align_corners=False)

def _as(obj, **defaults):
    if isinstance(obj, dict):
        cfg = dict(obj)
        params = dict(cfg.get('params', {}))
        for k, v in defaults.items():
            params.setdefault(k, v)
        cfg['params'] = params
        return build_from_cfg(cfg)
    return obj
```

### 10.5 让框架找到你的模块

在 `tools/train.py` 开头（或训练脚本里）加一行 `import my_modules`，注册装饰器就会执行，模块即可被配置引用。

---

## 11. 训练引擎、钩子与回调

### 11.1 用代码方式训练（不用命令行）

```python
from dlkit.engine.trainer import Trainer
from dlkit.utils.config import load_config

cfg = load_config('configs/unet_toy.yaml')
trainer = Trainer(cfg, work_dir='runs/my_run')
trainer.train()
```

### 11.2 钩子（Hook）：在训练过程中插入自定义逻辑

可用事件：`before_train` `after_train` `before_epoch` `after_epoch`
`before_train_step` `after_train_step`。

```python
def on_epoch(trainer, epoch=None, metrics=None, **kwargs):
    print(f'第 {epoch} 轮结束，mIoU = {metrics.get("mIoU"):.4f}')

trainer.register_hook('after_epoch', on_epoch)
trainer.train()
```

### 11.3 回调：早停 / 学习率自适应

```python
from dlkit.engine.callbacks import EarlyStopping, LRSchedulerOnPlateau

EarlyStopping(metric='mIoU', mode='max', patience=10).attach(trainer)
LRSchedulerOnPlateau(metric='mIoU', mode='max', factor=0.1, patience=5).attach(trainer)

trainer.train()
```

---

## 12. 可视化

### 12.1 训练曲线

```python
from dlkit.visualize.curves import plot_training

plot_training('runs/unet_toy/metrics.jsonl', 'runs/unet_toy/curves.png')
```

生成一张包含 loss 曲线和指标曲线的图。

### 12.2 分割结果对比图

```python
from dlkit.visualize.seg import plot_sample
import numpy as np
from PIL import Image

image = np.asarray(Image.open('data/toy/val/images/0000.png').convert('RGB'))
gt    = np.asarray(Image.open('data/toy/val/masks/0000.png'))
pred  = ...   # 预测的 mask（和 gt 同尺寸的类别编号数组）

plot_sample(image, gt, pred, palette=palette, names=names, save_path='compare.png')
```

> `palette` 和 `names` 可从 `data/toy/classes.json` 读取（predict 命令会自动处理）。

---

## 13. 部署：ONNX / TorchScript

```powershell
# 导出 ONNX（跨框架部署）
python tools/export.py --config configs/unet_toy.yaml `
    --checkpoint runs/unet_toy/best.pth `
    --format onnx --output runs/unet_toy/model.onnx

# 导出 TorchScript（PyTorch 生态内部署）
python tools/export.py --config configs/unet_toy.yaml `
    --checkpoint runs/unet_toy/best.pth `
    --format torchscript --output runs/unet_toy/model.pt
```

代码方式推理：

```python
from dlkit.deploy.infer import Inferencer
from dlkit.utils.config import load_config

cfg = load_config('configs/unet_toy.yaml')
inferencer = Inferencer(cfg, 'runs/unet_toy/best.pth',
                        palette='data/toy/classes.json')

result = inferencer.predict('some_image.png')      # 返回 {'logits', 'probs', 'mask'}
inferencer.predict_file('some_image.png', output_dir='runs/predict')  # 保存 mask 图 + 叠加图
```

---

## 14. 训练产物说明

每次训练都会在 `output_dir`（如 `runs/unet_toy/`）生成：

| 文件 | 内容 |
|------|------|
| `best.pth` | 按 `save_best` 指标选出的最优权重 |
| `last.pth` | 最后一轮的权重（可用于 `--resume` 续训） |
| `config.yaml` | 本次训练用的配置快照（可复现） |
| `metrics.jsonl` | 每个 epoch 的指标记录（画曲线用） |
| `log.txt` | 完整日志 |

**断点续训：**

```powershell
python tools/train.py --config configs/unet_toy.yaml --resume runs/unet_toy/last.pth
```

---

## 15. 常见问题 FAQ

**Q1：训练时报 `No module named 'dlkit'`？**
说明你不在项目根目录执行，或用了别的方式调用脚本。请在项目根目录下运行 `python tools/train.py ...`（工具脚本已自动处理路径）。

**Q2：`import matplotlib` 报错 `_c_internal_utils`？**
你的 matplotlib 装成了 Python 3.13 的版本（环境是 3.11）。修复：

```powershell
python -m pip install --force-reinstall matplotlib
```

**Q3：训练很慢？**
你的 torch 是 CPU 版。可以：① 减少数据量/epoch；② 装 CUDA 版 torch；③ 换更小的 backbone（`SimpleCNN` 的 channels 调小）。

**Q4：`num_classes` 怎么算？**
= 类别数（**包含背景**）。例如 3 种目标 + 背景 = 4。

**Q5：标签图该长什么样？**
单通道灰度图，像素值就是类别编号。用 `labelme`、`labelImg` 等工具标注后导出即可；别用彩色三通道图当标签。

**Q6：`ignore_index: 255` 是什么？**
标签里值为 255 的像素会被忽略（不算损失、不算指标）。如果你的数据没用到，删掉这个参数即可。

**Q7：想换评价指标或损失，要改代码吗？**
不用。改配置里的 `type` 即可；如果内置没有，按第 10 节自己写一个注册进去。

**Q8：报错 `not found in any registry`？**
说明配置里写的 `type` 名字拼错了，或对应模块没被 import。检查拼写，或确认 `import my_modules` 已执行。

---

## 附：推荐学习路线

1. 用合成数据跑通 UNet，看懂 `runs/unet_toy/` 里的每个产物。
2. 把 `model.type` 依次换成 `DeepLabV3`、`DeepLabV3Plus`，对比结构差异（看参数数量变化）。
3. 换 backbone（`SimpleCNN` → `ResNet` → `TorchvisionBackbone`），理解预训练的作用。
4. 改 loss（CE → Dice → 组合）、改 scheduler，观察 `metrics.jsonl` 曲线变化。
5. 造一个自己的小数据集（哪怕几十张图），走一遍完整流程。
6. 写一个自己的 backbone 或 loss，体验「即插即用」。
