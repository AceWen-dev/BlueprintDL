# 如何用 AI 做好一个深度学习项目

> 适用场景：使用 Coding Agent 开发中大型深度学习项目，尤其适用于 BlueprintDL 这种基于注册表、Builder、依赖注入和配置驱动的项目锻造仓库。

## 阅读建议

这是一份可以反复查询的工程手册，不需要一次全部记住：

- 第一次阅读：重点看第 1、3、4、6、7、10 和 16 节；
- 准备给 AI 下任务：直接复制第 6 节的任务合同，或第 11 节的提示词模板；
- AI 已经开始跑偏：直接看第 12 节；
- 准备完善仓库制度：重点看第 9、10 和 14 节；
- 想理解背后的方法：阅读第 2、8、13 和 15 节。

## 1. 先说结论

AI 可以显著提高编码速度，但它不能代替工程治理。

如果只靠聊天记录告诉 AI“我以前说过什么”，项目越大，AI 越容易出现以下问题：

- 忘记早期约定；
- 在错误的目录新增代码；
- 为了解决局部问题破坏整体架构；
- 重复实现已有能力；
- 把项目专属代码放进通用框架；
- 顺手修改依赖、环境或无关代码；
- 代码看起来完成了，但没有可复现的验证结果。

解决办法不是写一个无限长的提示词，而是建立一套工程系统：

```text
你的设计意图
    ↓ 写入仓库
规则文档 + 架构边界 + 配置契约 + 测试 + 自动审计
    ↓ 每次任务都引用并验证
AI 在有限范围内实施
    ↓
测试、审查、交付门禁
```

最重要的原则是：

> 不要要求 AI 永远记住你的想法，要让仓库本身成为 AI 和人的共同记忆。

AI 更适合做一个受约束、可检查、可回滚的工程协作者，而不是一个拿到一句模糊需求后自由发挥的“全自动程序员”。

---

## 2. 为什么 AI 会“忘记”和乱放代码

### 2.1 对话不等于可靠的项目记忆

长对话会不断积累需求、讨论、旧方案和临时决定。随着内容增加，早期信息可能被压缩、弱化，或者与后来的要求产生冲突。即使 AI 仍然知道大致目标，也可能忘记某个具体目录规则或历史决定。

因此，下面这些内容不能只存在于聊天记录中：

- 项目定位；
- 架构分层；
- 目录所有权；
- 依赖方向；
- 命名规则；
- 禁止事项；
- 测试命令；
- 交付要求；
- 已经作出的关键设计决定。

### 2.2 模糊需求会产生“合理但不符合预期”的代码

例如只说：

> 帮我添加一个新的分割模型。

AI 并不知道：

- 这是 BlueprintDL Core 的通用模型，还是 `polypmeasure` 项目专属模型；
- 是否需要拆成 backbone、decoder 和 head；
- 使用哪个 Registry；
- 注册名和 `provider` 是什么；
- 是否需要 Builder 支持嵌套配置；
- 哪些测试才算完成；
- 能否修改现有配置格式。

缺少这些边界时，AI 只能自行假设。所谓“乱写”，很多时候其实是需求没有把决策权边界说清楚。

### 2.3 一次修改范围过大

“帮我把整个项目完善好”通常不是一个可安全执行的工程任务。它同时包含架构、数据、模型、训练、部署、文档和环境等多个决策，AI 很容易在没有足够依据时替你作出大量隐含选择。

更可靠的方式是把工作拆成可以独立验收的增量，例如：

1. 建立项目骨架和清单；
2. 定义数据输入输出契约；
3. 实现并注册 Dataset；
4. 实现模型组件；
5. 完善 Builder；
6. 接入 Pipeline；
7. 添加单元测试；
8. 添加 GPU 集成测试；
9. 审计并导出项目。

---

## 3. 建立“分层记忆系统”

不同信息的生命周期不同，不应该全部塞进一个 README 或一段提示词中。

| 记忆层 | 保存内容 | 推荐载体 | 更新时机 |
| --- | --- | --- | --- |
| 长期规则 | 架构哲学、目录边界、禁止事项、验证要求 | `AGENTS.md`、`standards/` | 规则确实发生变化时 |
| 架构事实 | 模块职责、依赖方向、对象构造流程 | `docs/ARCHITECTURE.md` | 架构发生变化时 |
| 项目身份 | 项目 ID、包名、Core 版本、插件入口、交付清单 | `project.yaml`、`pyproject.toml` | 项目契约变化时 |
| 设计决策 | 为什么选择或放弃某个方案 | `docs/decisions/` 下的 ADR | 重要决策作出时 |
| 当前任务 | 本次目标、范围、验收标准和非目标 | Issue、任务卡或临时任务说明 | 每个任务开始和结束时 |
| 可执行约束 | 行为是否正确、边界是否被破坏 | 测试、静态检查、Forge audit | 每次修改时 |

这里最关键的是区分“规则”和“事实”：

- “Core 不得导入具体项目”是长期规则；
- “PolypMeasure 使用 `polypmeasure` 作为 provider”是项目事实；
- “本次只添加 BoundaryLoss，不修改 Trainer”是当前任务范围；
- “为什么没有把所有项目加入同一个 uv workspace”是设计决策。

### 3.1 文档不是越多越好

文档必须有明确所有权，避免同一件事在五个文件中各写一份。建议为每类信息指定唯一权威来源：

- Python 依赖以 `pyproject.toml` 为准；
- 项目身份和交付清单以 `project.yaml` 为准；
- 架构规则以 `standards/PROJECT_STANDARD.md` 为准；
- 组件归属以 Registry 的 provenance 记录为准；
- 行为正确性以测试结果为准。

如果文档与代码可以表达同一条约束，优先把关键约束做成机器可执行的检查。因为 AI 可能误读文字，但测试失败会明确告诉它不能交付。

---

## 4. 用目录和依赖方向约束 AI

你的 BlueprintDL 仓库可以理解为四个区域：

```text
standards/       长期工程规则和交付门禁
dlkit/           BlueprintDL Core，可复用框架能力
projects/        具体项目，每个项目拥有独立边界
dist/            审计后生成的构建和交付产物
```

依赖方向必须保持为：

```text
具体项目  ──────>  BlueprintDL Core 的公开接口
Core       -X--->  具体项目
项目 A     -X--->  项目 B 的内部实现
交付项目   -X--->  仓库中未声明的隐藏文件
```

### 4.1 新代码到底应该放在哪里

每次让 AI 写代码前，先回答下面的问题：

1. 这个实现是否包含客户、数据集、标签、业务流程或具体任务假设？
2. 它是否已经被两个以上项目真实复用？
3. 它是否有稳定且与具体项目无关的接口？
4. 把它放进 Core 后，Core 是否需要反向依赖某个项目？

推荐判断规则：

| 代码类型 | 应放位置 |
| --- | --- |
| 通用 Registry、Builder、配置校验、基础训练能力 | `dlkit/` |
| 已证明可跨项目复用的 Dataset、Model、Loss、Metric | `dlkit/` |
| PolypMeasure 特有的数据格式、模型结构、损失或业务规则 | `projects/polypmeasure/src/polypmeasure/` |
| 项目训练、评估和推理配置 | 对应项目的 `configs/` |
| 项目测试 | 对应项目的 `tests/` |
| 尚不确定能否通用的组件 | 先留在具体项目，不要提前放进 Core |

一个实用原则是：

> 先项目化，再通用化；通过真实复用证明通用性，而不是凭想象把代码提前塞进 Core。

---

## 5. 把架构规则变成 AI 不容易破坏的契约

对于你的深度学习框架，可以固定以下责任边界。

### 5.1 Registry：只负责“名字对应谁”

Registry 应负责：

- 注册；
- 查找；
- 重名检查；
- 提供组件清单和来源信息。

Registry 不应该：

- 解析整个配置；
- 实例化复杂对象图；
- 开始训练；
- 加载数据集或权重；
- 初始化 CUDA；
- 保存已经构造好的有状态对象。

项目组件需要显式携带归属信息，例如：

```python
@LOSSES.register(
    name="BoundaryLoss",
    provider="polypmeasure",
    tags=("segmentation", "boundary"),
)
class BoundaryLoss:
    ...
```

这比单纯根据目录或类名猜测归属更加可靠。

### 5.2 Component：只实现自己的领域能力

组件应该遵守稳定接口，不应该知道整个 Pipeline 如何运行，也不应该自己去全局容器中寻找 logger、device 或 optimizer。

新增一个组件时，理想改动通常只有：

```text
组件实现 + 注册 + 配置 + 测试
```

如果新增一个 Loss 还需要修改 Trainer 中的大量 `if/elif`，说明插件边界没有真正建立起来。

### 5.3 Builder：解释配置并注入运行时依赖

配置中保存的是可序列化意图：

```yaml
optimizer:
  type: AdamW
  params:
    lr: 0.0003
    weight_decay: 0.01
```

配置中不应该保存：

- `model.parameters()`；
- 已构建的 optimizer；
- device 对象；
- logger 实例；
- 打开的文件或数据库连接；
- 密钥；
- 本机 CUDA 路径。

这些运行时对象由 Builder 或 Composition Root 显式注入。

因此，optimizer 和 scheduler 可以拥有专门的 Builder：optimizer 需要注入模型参数，scheduler 需要注入已经构建的 optimizer。专门 Builder 并不违反配置驱动，关键是“选择来自配置、依赖来自注入”。

### 5.4 Pipeline：只负责编排流程

Pipeline 应让人一眼看出训练或推理顺序：

```text
加载注册项
  -> 校验配置
  -> 构造数据流水线
  -> 构造模型、损失和指标
  -> 构造 optimizer(model parameters)
  -> 构造 scheduler(optimizer)
  -> 构造 Trainer
  -> 运行训练或评估
```

Pipeline 不应维护组件名字映射，也不应针对每个模型或优化器编写选择分支。

### 5.5 GPU 是运行策略，不是仓库硬编码

项目面向 GPU，不代表 AI 可以擅自安装 CUDA 或把某个 CUDA wheel 写死在通用规则中。

应该分开处理：

- 应用配置：期望使用 `cuda`、`auto`、混合精度或分布式策略；
- 运行时发现：当前机器是否有可用 GPU、rank 和能力信息；
- 包安装：部署环境选择兼容的 PyTorch/CUDA 组合；
- 容器部署：基础镜像、驱动接口、挂载路径和 GPU 权限。

让 AI 修改架构并不等于授权它修改本机 CUDA、替换 PyTorch 或下载模型权重。此类行为必须单独提出并得到明确确认。

---

## 6. 每次任务都使用“任务合同”

向 AI 下达任务时，至少写清楚六件事：

1. **目标**：完成后用户能获得什么；
2. **范围**：允许修改哪些模块；
3. **边界**：明确禁止修改什么；
4. **架构归属**：属于 Core 还是具体项目；
5. **验收标准**：什么结果算完成；
6. **验证方式**：必须运行哪些测试或审计命令。

推荐模板：

```text
任务目标：
在 polypmeasure 项目中新增 BoundaryLoss，并支持通过配置构造。

项目归属：
这是 polypmeasure 专属组件，不得放入 dlkit Core。

允许范围：
- projects/polypmeasure/src/polypmeasure/components/
- projects/polypmeasure/configs/
- projects/polypmeasure/tests/

工程约束：
- 遵守 standards/PROJECT_STANDARD.md；
- 使用 LOSSES Registry；
- provider 必须为 polypmeasure；
- 配置只能包含可序列化参数；
- 不修改 Trainer，不硬编码设备，不安装依赖；
- 保留工作区已有无关修改。

验收标准：
- 显式 bootstrap 后可以查询到该组件；
- Builder 可以从配置构造它；
- 重名或未知名称能清楚报错；
- 原配置对象不被修改；
- 项目测试和 Forge audit 通过。

验证命令：
- pytest projects/polypmeasure/tests -q
- uv run python tools/forge.py inspect polypmeasure
- uv run python tools/forge.py audit polypmeasure

交付要求：
先检查现有实现并说明计划，然后实施；最后列出修改文件、测试结果、假设和未完成项。
```

这样的任务描述不会限制 AI 的编码能力，反而会减少无意义的猜测和返工。

---

## 7. 要求 AI 遵循固定工作循环

### 阶段一：先调查，不立刻写代码

要求 AI 先完成：

- 查看项目结构；
- 阅读仓库级规则和目标项目 README；
- 找到同类实现；
- 查看配置格式、注册方式和现有测试；
- 检查 Git 工作区是否已有未提交修改；
- 说明计划修改的文件和原因。

如果 AI 连代码应该放在哪一层都还没有判断清楚，就不应该开始大规模修改。

### 阶段二：给出小而明确的计划

一个好的计划应该能映射到文件和验收标准，例如：

```text
1. 在项目组件目录实现 BoundaryLoss，并注册 provider。
2. 在 bootstrap 的显式导入列表中启用该模块。
3. 添加一份最小配置示例。
4. 测试直接调用、注册发现和配置构造。
5. 运行项目测试与 Forge audit。
```

“完善架构”“优化代码”不算可验证计划。

### 阶段三：小批量实施

建议一次任务只完成一个垂直切片。一个垂直切片应该从配置或入口一直连到实现和测试，但不要同时重构多个无关模块。

例如，新增一个 Dataset 可以包含：

- Dataset 实现；
- 注册；
- Builder 可构造性；
- 示例配置；
- 单元测试。

但不应该顺手把所有 Dataset、Trainer、Logger 和配置系统全部重写。

### 阶段四：验证而不是宣称

不要只接受“已经完成”“应该可以运行”这类结论。要求 AI 给出实际执行结果：

- 运行了什么命令；
- 多少测试通过；
- 哪些测试因环境条件跳过；
- 是否执行了构建；
- 是否执行了边界审计；
- 是否产生未预期的文件。

对于 GPU 项目，测试建议分层：

| 测试层 | 目的 | 是否必须占用 GPU |
| --- | --- | --- |
| 单元测试 | 验证 Registry、Builder、配置、算法局部行为 | 否 |
| CPU 小样本测试 | 验证对象图和基本前向流程 | 通常否 |
| GPU 集成测试 | 验证 CUDA、AMP、显存和真实性能路径 | 是，可按环境跳过 |
| 端到端实验 | 验证真实数据和训练目标 | 是，单独运行 |

项目面向 GPU，不等于所有架构测试都必须下载 CUDA 才能运行。单元测试可使用小型 fake 或当前环境已有能力；真正的 GPU 行为由独立集成测试验证。

### 阶段五：完成后更新共同记忆

如果任务改变了长期事实，必须同步更新对应权威文件：

- 新增项目：更新项目清单；
- 新增公开入口：更新 README；
- 改变架构决策：添加 ADR；
- 改变配置结构：更新 schema、示例和迁移说明；
- 新增项目组件：通过 Registry provenance 自动生成清单，不手写第二份列表；
- 改变交付边界：更新 `project.yaml` 并重新审计。

普通实现细节不需要全部写进长期文档。否则“记忆文件”会快速膨胀并失去作用。

---

## 8. 如何防止长对话导致上下文失控

### 8.1 一个对话尽量围绕一个清晰目标

不要在同一个任务中不断追加互不相关的需求，例如先改 Dataset，然后突然让它重构 Docker、编写部署平台、修改 Trainer，再解决另一个项目的 Bug。

可以把一个大目标拆成多个连续任务，但每个任务都应有独立验收结果。

### 8.2 每个阶段要求生成“交接摘要”

长任务阶段结束时，让 AI 总结：

```text
- 当前目标
- 已完成内容
- 修改文件
- 已确认的设计决定
- 测试结果
- 未解决问题
- 下一步建议
- 禁止被后续任务破坏的约束
```

但交接摘要仍然只是临时信息。真正长期有效的规则要回写仓库。

### 8.3 新会话从仓库重新建立上下文

开始新会话时，不需要复制全部旧聊天，可以要求：

```text
先阅读仓库规则、架构文档、目标项目清单和相关测试，检查 Git 状态，
然后总结你理解的项目边界、当前任务范围和计划。在我要求的范围内继续工作。
```

这相当于让新 AI 从稳定事实恢复上下文，而不是依赖它“记得以前聊过什么”。

### 8.4 已完成决策不要反复讨论

重要决定一旦形成，应记录为 ADR。例如：

```text
docs/decisions/0001-independent-project-environments.md
```

ADR 至少说明：

- 背景；
- 最终决定；
- 为什么这样决定；
- 放弃了哪些方案；
- 可能带来的后果；
- 哪些条件出现时可以重新评估。

以后 AI 先阅读 ADR，就不需要每次重新猜测为什么没有使用统一 workspace。

---

## 9. 将约束自动化

只写“请不要乱放代码”几乎没有效果。应把它转换成可检查规则。

### 9.1 可自动检查的约束示例

- Core 包构建物不能包含 `projects/`；
- Core 不能导入项目包；
- 项目 A 不能导入项目 B；
- 每个项目必须有 `project.yaml` 和 `pyproject.toml`；
- bootstrap 必须属于项目自己的 Python 包；
- 项目组件必须声明正确的 `provider`；
- 交付路径必须是相对路径且不能越界；
- 导出物不能包含 `.venv`、缓存、数据集、checkpoint 或密钥；
- 配置中不得出现不可序列化运行时对象；
- 新插件不得要求修改中央 Pipeline 的类型分支。

### 9.2 当前仓库已有的检查入口

```powershell
# 查看有哪些具体项目
uv run python tools/forge.py list

# 查看 Core 或某个 provider 提供的组件
uv run python tools/forge.py components --provider blueprintdl

# 查看项目组件归属
uv run python tools/forge.py inspect polypmeasure

# 检查项目身份、边界、bootstrap、跨项目导入和组件归属
uv run python tools/forge.py audit polypmeasure

# 审计通过后生成独立交付目录
uv run python tools/forge.py export polypmeasure dist/deliverables
```

以后每增加一种常见错误，都可以考虑增加一条测试或审计规则。工程约束应该随着真实问题演化，而不是一开始设计一个无所不包的治理系统。

---

## 10. Definition of Done：什么才算真正完成

建议将以下清单作为一般任务的完成标准，根据任务类型删减不适用项。

### 功能

- [ ] 实现满足明确需求，而不是只通过表面示例；
- [ ] 异常输入有清楚的错误信息；
- [ ] 没有引入隐藏的全局状态；
- [ ] 没有依赖未声明的本机文件。

### 架构

- [ ] 代码放在正确的 Core 或项目边界；
- [ ] 依赖方向没有反转；
- [ ] 新组件通过已有扩展点接入；
- [ ] 配置只保存可序列化信息；
- [ ] 运行时对象通过依赖注入提供；
- [ ] Pipeline 没有新增具体类型分支；
- [ ] 项目组件具有正确 provider。

### 环境

- [ ] 未擅自安装或替换 CUDA、PyTorch、驱动或系统运行时；
- [ ] 未把某台机器的绝对路径写进通用配置；
- [ ] 项目依赖记录在自己的 `pyproject.toml`；
- [ ] GPU 集成要求有明确说明和跳过策略。

### 验证

- [ ] 新增或修改了相关测试；
- [ ] 相关测试实际运行并通过；
- [ ] Core 与具体项目测试分别运行；
- [ ] Forge audit 通过；
- [ ] 必要时 package build 和导出验证通过。

### 交付

- [ ] 文档与公共配置示例同步；
- [ ] 没有夹带缓存、虚拟环境、数据和密钥；
- [ ] 最终报告列出修改文件、验证结果、假设和剩余风险；
- [ ] 没有擅自覆盖用户已有修改或提交 Git 历史。

---

## 11. 四种常用提示词模板

### 11.1 接手一个新项目

```text
我要在 BlueprintDL 锻造仓库中接入一个名为 <project-id> 的深度学习项目。

先不要写业务代码。请先：
1. 阅读仓库工程标准和 Forge 规则；
2. 检查现有目录、Git 状态和相似项目；
3. 明确 Core 与该项目的边界；
4. 列出项目的数据、模型、损失、指标、训练、推理和交付需求中仍缺少的信息；
5. 给出分阶段实施计划和每阶段验收标准。

约束：项目必须可独立打包和测试；项目组件带 provider；不加入统一 GPU
依赖环境；不安装 CUDA 或下载权重；不修改无关项目。
```

### 11.2 新增功能

```text
目标：<一句话描述可观察结果>
归属：<BlueprintDL Core / 具体项目 ID>
允许修改：<目录或模块>
禁止修改：<目录、公共接口、环境等>
架构要求：<Registry/Builder/DI/Pipeline 约束>
验收标准：<功能、异常、测试、审计>
验证命令：<实际命令>

请先调查同类实现并报告计划，再以最小范围实施。不要顺手重构无关代码。
```

### 11.3 排查 Bug

```text
现象：<完整错误、输入和环境>
预期：<正确行为>
最近变化：<相关提交或文件>
复现方式：<命令或最小示例>

请先诊断根因并给出代码证据，不要立即修改。区分根因、触发条件和表面症状。
确认根因后，再提出最小修复方案、回归测试和可能影响范围。
```

如果你希望它直接修复，把最后一句改成：

```text
确认根因后直接实施最小修复，并运行回归测试；不要扩大到无关重构。
```

### 11.4 架构审查或交付审查

```text
这是审查任务，不要修改文件。

请检查：
- Core 与项目边界；
- 跨项目导入；
- Registry 归属；
- Builder 与依赖注入；
- Pipeline 是否包含具体类型分支；
- 配置中是否混入运行时对象或机器路径；
- GPU/CUDA 是否被错误写死；
- 测试、构建和交付清单是否完整。

按严重程度报告问题。每个问题提供文件位置、原因、影响和最小修复建议。
没有证据的问题不要猜测。
```

---

## 12. 当 AI 开始跑偏时怎么处理

发现 AI 乱放代码时，不要继续追加更多零散要求让它“边写边改”。应立即暂停实现，要求它重新建立边界。

可以这样说：

```text
暂停继续编码。请先审计当前未提交修改：
1. 逐个文件说明它属于 Core 还是哪个具体项目；
2. 对照工程标准找出放错位置、越界依赖和重复实现；
3. 区分我的原有修改与你本次修改；
4. 给出最小纠正计划；
5. 在我没有要求前，不要删除、重置或覆盖已有文件。
```

纠偏时尤其注意：

- 不要使用破坏性 Git 命令恢复整个工作区；
- 不要因为一个文件放错位置就顺手重构全部目录；
- 不要覆盖用户已有未提交改动；
- 先保留行为，再迁移位置；
- 迁移后重新运行原测试和边界审计。

---

## 13. 常见反模式

### 反模式一：把 AI 当成会永久记忆的同事

错误做法：

> 我之前已经说过了，你以后记住就行。

更好做法：把规则写入仓库的权威文件，并用测试或 audit 固化关键约束。

### 反模式二：只描述实现方式，不描述目标

错误做法：

> 给这里加一个工厂，再加一个基类。

更好做法：先说明需要替换什么实现、为什么需要配置选择、完成后应该观察到什么行为。Registry、Builder 和基类只是可能的手段。

### 反模式三：为“以后可能需要”提前抽象一切

注册表应该服务于真实扩展点，不要把每个工具函数、数据结构和异常类都注册化。过度抽象会让人和 AI 都难以判断真实流程。

### 反模式四：把所有项目放进一个依赖环境

多个项目共用 Git 仓库，不代表必须共用一套 Python、PyTorch 和 GPU 依赖。客户环境或 CUDA 组合可能不同，具体项目应保留独立锁定能力。

### 反模式五：只看代码，不验证交付

源仓库中能运行，可能只是因为 `PYTHONPATH`、根目录文件或其他项目恰好存在。最终应检查独立构建物或导出目录，而不仅是当前工作区。

### 反模式六：要求 AI 一次性重写整个系统

大规模重写会同时改变太多变量，很难判断行为变化来自哪里。优先使用兼容、可验证、可回滚的增量迁移。

### 反模式七：把大量生成代码误认为项目进度

真正的进度是已经关闭的风险和已经验证的能力，不是新增了多少文件。一个稳定的小切片比十个未经验证的模块更有价值。

---

## 14. 推荐为本仓库逐步补齐的治理文件

当前仓库已经有 `standards/PROJECT_STANDARD.md`、项目清单、Forge 工具和各类使用说明。后续可以按实际需要补充，而不是一次全部创建。

```text
AGENTS.md
    给 Coding Agent 的简明仓库级强制规则，只放高频且长期有效的内容。

docs/ARCHITECTURE.md
    描述 Core、项目、Registry、Builder、Pipeline 和 Composition Root 的关系。

docs/decisions/
    保存关键架构决策，例如独立项目环境、交付模式和组件晋升规则。

projects/<id>/README.md
    描述具体项目目标、数据契约、运行方式和限制。

projects/<id>/project.yaml
    项目身份、Core 兼容范围、bootstrap 和交付白名单。

projects/<id>/tests/
    把项目行为和边界变成可执行约束。
```

`AGENTS.md` 应保持短小。适合放入的规则包括：

- 修改前必须阅读哪些文件；
- Core 和项目的依赖边界；
- 新组件如何注册 provider；
- 禁止修改或下载哪些环境内容；
- 必须保留用户未提交修改；
- 任务完成前必须运行哪些检查；
- 不确定归属时先报告，不自行创建新层级。

详细原理仍然放在标准和架构文档中，`AGENTS.md` 只引用它们，避免复制整份说明。

---

## 15. 我对“用 AI 做项目”的理解

AI 最擅长的是：

- 快速阅读和关联大量局部代码；
- 根据清晰契约生成实现和测试；
- 执行机械迁移；
- 枚举边界情况；
- 对照规则进行审查；
- 在已有架构中补齐一个垂直切片。

AI 最容易出错的是：

- 在缺乏业务背景时替你作长期架构决策；
- 把临时方案当成永久规则；
- 混淆 Core、项目和环境的边界；
- 在大范围修改中忽略早期约束；
- 用“代码更多”掩盖“验证不足”。

因此，人的核心职责并没有消失，而是从“亲手敲每一行代码”转变为：

- 定义目标；
- 决定边界；
- 维护架构原则；
- 明确验收标准；
- 判断取舍；
- 审查证据和最终交付。

AI 的职责是：

- 先理解当前事实；
- 在授权范围内实施；
- 遵守仓库契约；
- 用测试证明结果；
- 清楚报告假设、风险和未完成项。

我认为高质量 AI 工程不是“写一个神奇提示词，让 AI 一次完成项目”，而是建立一个稳定的反馈系统：

```text
清晰目标
  -> 明确边界
  -> 小步实施
  -> 自动验证
  -> 人工判断
  -> 记录决定
  -> 进入下一轮
```

当这个循环建立起来后，即使更换 AI、开启新会话，或者项目持续开发几个月，新的 Agent 仍然可以通过仓库恢复正确上下文。此时项目质量不再依赖某一次对话有多长，也不依赖 AI 是否“记得你说过的话”。

最终目标不是让 AI 变得不会犯错，而是做到：

> AI 即使暂时遗忘、误解或尝试了错误方向，工程系统也能尽早发现、限制影响，并帮助它回到正确轨道。

---

## 16. 最简实践清单

如果暂时不想建立很多制度，先坚持下面十条：

1. 每个任务明确目标、范围、非目标和验收标准；
2. 修改前让 AI 阅读规则、相似实现、测试和 Git 状态；
3. 永久规则写进仓库，不依赖聊天记忆；
4. 先判断代码属于 Core 还是具体项目；
5. 一次只做一个可验证的垂直切片；
6. 不允许 AI 顺手修改环境和无关模块；
7. 关键约束尽量写成测试或审计；
8. 要求实际运行验证命令，不接受“理论上可以”；
9. 阶段结束记录设计决定和未完成项；
10. 交付时从独立构建物验证，不只在开发仓库里验证。

坚持这十条，你与 AI 的关系就会从“反复提醒和返工”，逐渐变成“有规则、有证据、可持续的工程协作”。
